import { NpmProvider as NpmProvider_ } from "@cliffy/upgrade/provider/npm";
import type {
  NpmProviderOptions as NpmProviderOptions_,
} from "@cliffy/upgrade/provider/npm";

/** @deprecated Import `NpmProvider` from `@cliffy/upgrade/provider/npm` instead. Will be removed in 2.0. */
export const NpmProvider = NpmProvider_;
/** @deprecated Import `NpmProvider` from `@cliffy/upgrade/provider/npm` instead. Will be removed in 2.0. */
export type NpmProvider = NpmProvider_;
/** @deprecated Import `NpmProviderOptions` from `@cliffy/upgrade/provider/npm` instead. Will be removed in 2.0. */
export type NpmProviderOptions = NpmProviderOptions_;
